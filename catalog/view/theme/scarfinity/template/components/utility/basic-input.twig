{% macro BasicInput(props) %}
    <input
        class="sInput{{props.valid == true ? " sInput--success" : props.valid == false ? " sInput--error": ""}}"
        type="{{props.type}}"
        name="{{props.name}}"
        value="{{props.value}}"
        placeholder="{{props.placeholder}}"
        {{props.disabled ? disabled: ''}}
        {{props.readonly ? readonly : ''}} />
{% endmacro %}