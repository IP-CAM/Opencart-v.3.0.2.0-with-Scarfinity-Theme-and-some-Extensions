{% import 'scarfinity/template/components/utility/basic-input.twig' as basicInput %}

{{ 
    basicInput.basicInput({
        label: entry_firstname,
        value: firstname,
        name: 'firstname',
        group: 'customer',
        type: 'text',
        placeholder: entry_firstname,
        required: true
    })
}}
{{ 
    basicInput.basicInput({
        label: entry_lastname,
        value: lastname,
        name: 'lastname',
        group: 'customer',
        type: 'text',
        placeholder: entry_lastname,
        required: true
    })
}}
{{ 
    basicInput.basicInput({
        label: entry_telephone,
        value: telephone,
        name: 'telephone',
        group: 'customer',
        type: 'phone',
        placeholder: entry_telephone,
        required: true
    })
}}
{{ 
    basicInput.basicInput({
        label: entry_email,
        value: email,
        name: 'email',
        group: 'customer',
        type: 'email',
        placeholder: entry_email
    })
}}
{{ 
    basicInput.basicInput({
        value: address_1,
        name: 'address_1',
        type: 'hidden',
        placeholder: entry_address_1
    })
}}
{{ 
    basicInput.basicInput({
        value: city,
        name: 'city',
        type: 'hidden',
        placeholder: entry_city
    })
}}
{{ 
    basicInput.basicInput({
        value: postcode,
        name: 'postcode',
        type: 'hidden',
        placeholder: entry_postcode
    })
}}
{{ 
    basicInput.basicInput({
        value: country_id,
        name: 'country_id',
        type: 'hidden',
        placeholder: entry_country
    })
}}
{{ 
    basicInput.basicInput({
        value: zone_id,
        name: 'zone_id',
        type: 'hidden',
        placeholder: ''
    })
}}

<script type="text/javascript">
  var inputs = $('.sInput[data-group="customer"]').each(function() {
    switch($(this).attr('name')) {
      case 'firstname': $(this).val('{{firstname}}').trigger('input'); break;
      case 'lastname': $(this).val('{{lastname}}').trigger('input'); break;
      case 'telephone': $(this).val('{{telephone}}').trigger('input'); break;
      case 'email': $(this).val('{{email}}').trigger('input'); break;
    }
  });
</script>
